# Core data science
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0
matplotlib>=3.7.0

# HTTP
requests>=2.31.0

# Dashboard
streamlit>=1.35.0
folium>=0.15.0
streamlit-folium>=0.16.0

# LLM (optional — only needed if users supply their own API key)
anthropic>=0.50.0
openai>=1.10.0

# Environment
python-dotenv>=1.0.0

# ── Optional: satellite imagery via Microsoft Planetary Computer ──────────
# Install these only if you want to enable real Sentinel-2 NDVI layers.
# They are NOT required for the core dashboard to work.
# pystac-client>=0.7.0
# planetary-computer>=0.5.0
# rioxarray>=0.15.0
# xarray>=2023.1.0
